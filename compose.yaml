services:
  qalens:
    build:
      context: .
    image: qalens:local
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - qalens-data:/data
    restart: unless-stopped

volumes:
  qalens-data:
